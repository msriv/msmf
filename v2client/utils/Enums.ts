export enum FacilityCategories {
  Space = "Space",
  Storage = "Storage",
  Instruments = "Instruments",
  CellCultureFacility = "Cell Culture Facility",
}

export enum BookingDuration {
  PerRun = "Per Run",
  PerHour = "Per Hour",
  PerDay = "Per Day",
  PerMonth = "Per Month",
}

export enum Roles {
  Visitor = "Visitor",
  Editor = "Editor",
  Admin = "Admin",
}

export enum SessionStorageKeys {
  UserToken = "user_token",
}

export enum LocalStorageKeys {
  UserIdentifier = "user_identifier",
}
